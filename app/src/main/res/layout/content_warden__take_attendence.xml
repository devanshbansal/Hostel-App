<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" style="@style/matchWidthheightMatch"    app:layout_behavior="@string/appbar_scrolling_view_behavior"

    tools:context="com.ektastudio.engggirl.hostliz.Warden_TakeAttendence">
<include
layout="@layout/header_menu"
    android:id="@+id/header"

    ></include>

    <LinearLayout
      style="@style/matchWidthWrapHeight"
        android:orientation="horizontal"
        android:layout_below="@+id/header"
        android:layout_marginTop="20dp"
        android:padding="10dp"
        android:id="@+id/textheader"
        >
        <TextView
           style="@style/wrapWidthheightWrap"
            android:id="@+id/TextHead"

            android:text="@string/userHead"
            android:textStyle="bold"
            android:textColor="#795548"
            android:layout_marginBottom="10dp"
            />

        <TextView
            style="@style/wrapWidthheightWrap"
            android:id="@+id/TextWarden"
            android:text=""
            android:textStyle="bold"
            android:textColor="#795548"
            android:layout_marginBottom="10dp"
            android:layout_marginLeft="5dp"
            />
    </LinearLayout>
    <TableLayout
        style="@style/wrapWidthheightWrap"
        android:id="@+id/TableLayoutData"
        android:padding="10dp"
     android:layout_below="@+id/textheader"
        android:layout_marginTop="2dp"
        ></TableLayout>

</RelativeLayout>
